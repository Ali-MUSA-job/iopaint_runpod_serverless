# PyTorch with CUDA support (install first)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.0
torchvision==0.18.0

# Core scientific computing
numpy
scipy
matplotlib
einops

# Fixed versions to match IOPaint requirements
fastapi==0.108.0
markupsafe==2.0.23
websockets==11.0.3
requests
huggingface_hub==0.25.2

# IOPaint (install after fixing dependencies)
iopaint

# RunPod (install last)
runpod>=1.6.0
