# Core
numpy
torch==2.3.0
torchvision==0.18.0
--extra-index-url https://download.pytorch.org/whl/cu121

# Include IOPaint via PyPI (which already includes its dependencies)
iopaint

# Additional dependencies not bundled in IOPaint
scipy
matplotlib
einops
requests
huggingface_hub==0.25.2
runpod
